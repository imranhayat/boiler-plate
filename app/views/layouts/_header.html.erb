<nav class="navbar navbar-toggleable-md navbar-light bg-faded fixed-top">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href="<%= root_path %>"><h2>yourappname</h2></a>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
            <a class="nav-link" href="<%= destroy_user_session_path %>" data-method="delete">Log Out</a>
        <% else %>
            <a class="nav-link" href="<%= new_user_registration_path %>">Sign Up</a>
        <% end %>
      </li>
        <% if user_signed_in? %>
      
          <li class="nav-item">
            <a class="nav-link" href="<%= edit_user_registration_path %>">Settings</a>
          </li>
          
          <li class="nav-item">
            <a class="nav-link" href="<%= edit_user_path(current_user) %>">Profile</a>
          </li>
          
          <% if current_user.admin %>
            <li class="nav-item">
              <a class="nav-link" href="<%= admin_panel_path %>">Admin Dashboard</a>
            </li>
          <% end %>
          
        <% end %>
      <!--<li class="nav-item">-->
      <!--  <a class="nav-link disabled" href="#">Disabled</a>-->
      <!--</li>-->
    </ul>
  </div>
</nav>